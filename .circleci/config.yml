version: 2.1
orbs:
  linter: talkiq/linter@4.0.1
  node: circleci/node@7.2.1

workflows:
  run-jobs:
    jobs:
      - linter/pre-commit
      - node/run:
          npm-run: build
          name: build-site
